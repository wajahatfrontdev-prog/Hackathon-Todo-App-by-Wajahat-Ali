# Backend Environment Configuration
# Copy this file to .env and fill in the values

# Neon PostgreSQL Database Connection URL
# Format: postgres://username:password@host/database?sslmode=require
# Get this from your Neon dashboard: https://console.neon.tech
DATABASE_URL=postgres://user:password@ep-xxx.us-east-1.aws.neon.tech/todoapp?sslmode=require

# JWT Authentication Secret
# IMPORTANT: This must match BETTER_AUTH_SECRET in frontend/.env
# Generate a secure random string (at least 32 characters)
# Example: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secure-secret-key-min-32-characters

# CORS Origins
# Comma-separated list of allowed origins for cross-origin requests
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Groq API Key for Phase III AI Chat
# Get this from: https://console.groq.com/keys
GROQ_API_KEY=gsk_
